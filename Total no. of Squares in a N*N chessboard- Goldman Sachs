class Solution {
    static Long squaresInChessBoard(Long N) {
        // code here
        long ans;
        if(N==1)
            ans=1;
        else
        {
            long n1=N*(N+1);
            ans=((n1/2)*(2*N+1))/3;
        }
        return ans;
    }
